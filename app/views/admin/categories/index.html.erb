<div class="row">
  <div class="col-md-offset-11">
    <%= link_to "New", new_admin_category_path, class: "btn btn-primary" %>
  </div>
</div>

<% if @categories.blank? %>
  <p>Click the New button to create a new Category </p>
<% else %>

<%= render 'categories', categories: @categories%>

<% end %>